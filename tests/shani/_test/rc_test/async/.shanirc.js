export default async (ctx) => {
    await adone.promise.delay(100);

    ctx.before(function () {});

    ctx.beforeEach(function () {});

    ctx.afterEach(function () {});

    ctx.after(function () {});
};
